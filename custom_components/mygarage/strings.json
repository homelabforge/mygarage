{
  "config": {
    "step": {
      "user": {
        "title": "Connect to MyGarage",
        "description": "Use your MyGarage base URL and a Widget API key (Settings → Integrations). Optionally add the inbound webhook token for write services.",
        "data": {
          "host": "Host URL",
          "api_key": "Widget API key",
          "webhook_token": "Webhook ingest token (optional)"
        }
      }
    },
    "error": {
      "cannot_connect": "Could not connect or the API key was rejected",
      "unknown": "Unexpected error"
    },
    "abort": {
      "already_configured": "This MyGarage host is already configured"
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "MyGarage options",
        "data": {
          "host": "Host URL",
          "api_key": "Widget API key",
          "webhook_token": "Webhook ingest token (optional)"
        }
      }
    }
  },
  "entity": {
    "sensor": {
      "odometer": { "name": "Odometer" },
      "fuel_economy": { "name": "Fuel economy" },
      "overdue_maintenance": { "name": "Overdue maintenance" },
      "upcoming_maintenance": { "name": "Upcoming maintenance" },
      "last_fuel_date": { "name": "Last fuel date" },
      "engine_hours": { "name": "Engine hours" }
    },
    "binary_sensor": {
      "maintenance_due": { "name": "Maintenance due" }
    }
  }
}

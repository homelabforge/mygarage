# MyGarage Environment Configuration Example
# Copy this file to .env and customize for your deployment
#
# ==============================================================================
# IMPORTANT: Most settings are configurable through the web UI!
# ==============================================================================
# After first startup, configure these in Settings → System:
#   - Timezone, date/time formats
#   - Authentication mode (none/local/oidc)
#   - Multi-user support
#   - Display units (miles vs km, gallons vs liters)
#
# This .env file is only for settings that:
#   1. Must be set BEFORE first startup (database URL, OIDC secrets)
#   2. Cannot be changed through the UI for security reasons
#
# Most users can run MyGarage with ZERO configuration - just start the container!

# ==============================================================================
# DATABASE (Optional - defaults to SQLite)
# ==============================================================================
# Database connection URL
# Default: SQLite at /data/mygarage.db (zero-config, works out of the box)
#
# PostgreSQL (recommended for multi-user or high-traffic deployments):
# MYGARAGE_DATABASE_URL=postgresql+asyncpg://username:CHANGEME_PASSWORD@hostname:5432/mygarage

# ==============================================================================
# OIDC/SSO AUTHENTICATION (Required only if using OIDC)
# ==============================================================================
# These MUST be set as environment variables if using auth_mode=oidc
# Cannot be configured through the UI for security reasons
#
# After setting these, change auth_mode to 'oidc' in Settings → System
#
# OIDC Client ID (from your identity provider)
# MYGARAGE_OIDC_CLIENT_ID=mygarage
#
# OIDC Client Secret (from your identity provider)
# ⚠️  SECURITY: Never commit this to version control!
# MYGARAGE_OIDC_CLIENT_SECRET=CHANGEME_YOUR_CLIENT_SECRET
#
# OIDC Discovery URL (provider's .well-known/openid-configuration endpoint)
# Examples:
#   Authentik: https://auth.example.com/application/o/mygarage/.well-known/openid-configuration
#   Keycloak: https://keycloak.example.com/realms/myrealm/.well-known/openid-configuration
#   Google: https://accounts.google.com/.well-known/openid-configuration
#   Azure AD: https://login.microsoftonline.com/{tenant-id}/v2.0/.well-known/openid-configuration
# MYGARAGE_OIDC_DISCOVERY_URL=https://auth.example.com/.well-known/openid-configuration

# ==============================================================================
# CORS (Optional - for custom domains)
# ==============================================================================
# Allowed CORS origins (comma-separated list)
# Default: localhost development ports
# Only needed if accessing from custom domains
#
# MYGARAGE_CORS_ORIGINS=https://garage.example.com,https://app.example.com

# ==============================================================================
# DEBUG MODE (Optional - for development only)
# ==============================================================================
# Enable debug logging and detailed error messages
# ⚠️  WARNING: Never enable in production - exposes sensitive information!
# Default: false
#
# MYGARAGE_DEBUG=true

# ==============================================================================
# ADVANCED: Override Auto-Generated Defaults (Rarely Needed)
# ==============================================================================
# These have sensible defaults and usually don't need to be changed
#
# JWT secret key (auto-generated on first startup and saved to /data/secret.key)
# ⚠️  Only set this if you need to share secrets across multiple instances
# MYGARAGE_SECRET_KEY=your-secret-key-here
#
# JWT cookie secure flag (auto-detect: true in production, false in debug mode)
# Options: auto (default), true (HTTPS only), false (HTTP allowed)
# MYGARAGE_JWT_COOKIE_SECURE=auto
#
# API rate limits (requests per time period)
# MYGARAGE_RATE_LIMIT_DEFAULT=200/minute
# MYGARAGE_RATE_LIMIT_UPLOADS=20/minute
# MYGARAGE_RATE_LIMIT_AUTH=5/minute
# MYGARAGE_RATE_LIMIT_EXPORTS=5/minute
#
# Maximum upload sizes in MB
# MYGARAGE_MAX_UPLOAD_SIZE_MB=10
# MYGARAGE_MAX_DOCUMENT_SIZE_MB=25
# MYGARAGE_MAX_CSV_SIZE_MB=10
#
# Server host and port (usually set via Docker, not needed in .env)
# MYGARAGE_HOST=0.0.0.0
# MYGARAGE_PORT=8686
